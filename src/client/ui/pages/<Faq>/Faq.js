export default (client, id) => {  return client.hoc({    id,    classes: {      wrapper: `        display: flex;        flex-flow: column;        align-items: center;        text-align: center;        padding-bottom: 25vh;      `,      question: `        font-size: 24px;        font-weight: bold;        padding: 40px 20px;        margin-top: 50px;        color: ${client.lib.Colors.BLUE_HEADLINE};      `,      answer: `        font-size: 20px;        padding: 20px;      `,    },    render({ classes }) {      return (        <div id='faq-wrapper' class={classes.wrapper}>          {            client.lib.Faq.map((item, i) => {              const { text, className } = item;              return (                <div id={`faq-${i}`} class={classes[className]}>                  {text}                </div>              );            })          }        </div>      );    }  });};