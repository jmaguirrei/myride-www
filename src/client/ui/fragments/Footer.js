export default (client, id) => {  return client.hoc({    id,    classes: {      wrapper: `        padding: 50px 20px 80px;        background: ${client.lib.Colors.GREY_DARKEST};      `,      logo: `        width: 60%;      `,      text: `        padding: 10px 0;        color: white;      `,    },    render({ classes }) {      return (        <div id='footer' class={classes('wrapper')}>          {            client.lib.Footer.map(item => {              const { type, url, text, className } = item;              if (type === 'image') return <img src={url} class={classes(className)}/>;              return <div class={classes(className)}>{text}</div>;            })          }        </div>      );    }  });};